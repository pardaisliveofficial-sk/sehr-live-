rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global Safety Net: allow read and write for server and clients
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
